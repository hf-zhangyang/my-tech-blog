import sfcCompiler from '@vue/compiler-sfc';
interface ResolvedVuePaths {
    vue: string;
    version: string;
    hasLocalVue: boolean;
    compiler: string;
}
export declare function resolveVue(root: string, isBuild?: boolean): ResolvedVuePaths;
export declare function resolveCompiler(cwd: string): typeof sfcCompiler;
export {};
