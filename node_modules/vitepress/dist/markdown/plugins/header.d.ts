import MarkdownIt from 'markdown-it';
import { MarkdownParsedData } from '../markdown';
export interface Header {
    level: number;
    title: string;
    slug: string;
}
export declare const extractHeaderPlugin: (md: MarkdownIt & {
    __data: MarkdownParsedData;
}, include?: string[]) => void;
