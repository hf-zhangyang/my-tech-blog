import { MarkdownOpitons } from './markdown/markdown';
import { PageData } from './config';
interface MarkdownCompileResult {
    vueSrc: string;
    pageData: PageData;
}
export declare function createMarkdownToVueRenderFn(root: string, options?: MarkdownOpitons): (src: string, file: string, lastUpdated: number, injectData?: boolean) => MarkdownCompileResult;
export {};
